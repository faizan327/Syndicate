import 'package:flutter/material.dart';

final ColorScheme lightColorScheme = const ColorScheme(
  brightness: Brightness.light,
  primary: Color(0xFFE49C3F), // Primary brand color
  secondary: Color(0xFFEF6C00), // Secondary accent color
  background: Colors.white, // Background color
  surface: Color(0xFFF5F5F5), // Card & surfaces
  onPrimary: Color(0xFFE0E0E0), // Text/Icon on primary color
  onSecondary: Colors.black, // Text/Icon on secondary color
  onBackground:Color(0xfffff5e7), // Text on background
  onSurface: Colors.black, // Text/Icon on surface
  error: Colors.red, // Error color
  onError: Colors.white, // Text/Icon on error color

);
